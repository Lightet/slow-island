# 动作修正验证记录

2026-09-13，本机 Chrome。此次以动画中间帧及实际拖动为依据，替换此前仅凭互动状态和静止截图作出的过度结论。

## 修正内容

- 移除头、身体、尾巴的分切动画，使用一张完整透明纹理做连续网格形变。抚摸时颈部过渡连续，脚底顶点不随转头移动；图集先裁出单只动物再形变，避免带出相邻角色。
- 写实手替换为卡通 SVG 手。四根手指与拇指有独立结构，手腕和指节随抚摸弯曲；指尖定位跟随鼠标，自动抚摸使用各动物单独的接触位置。
- 零食对准各自嘴部，大小随动物比例缩放；船和秋千移动时重新计算位置。补上猫玩羽毛棒时的道具显示。
- 可可热气从杯口升起，信封从企鹅手中的信件附近出发。倒茶水流随壶嘴移动，浇水随水壶喷头移动；缩短狐狸尾巴旁的木棍，柚子落水避开水豚，涟漪裁切在池面内。

## 实际检查

- `scripts/motion-check.html` 通过页面按钮和键盘事件触发正常互动，将当前 WebGL 画布及 SVG 场景冻结为分帧图。采样抚摸、玩耍、喂食和场景动作，目标时间点为约 0、0.45、0.95、1.6、2.3、3.4 秒；查看了 10 个场景及兼容骑行页的分帧图。
- 初次检查发现 WebGL 着色器精度不一致导致静态回退，已修正并重新录制。随后逐页确认 11 个互动页均使用 `continuous-mesh` 渲染。
- 桌面猫：实际向左、向右拖动抚摸，查看手指跟随、闭眼歪头和颈部轮廓；暂停时头部回到中立，恢复后可以继续。
- 390 × 844 窄视口：实际拖动抚摸猫，页面无横向溢出。实际拖动船后喂食、拖动秋千后抚摸，查看食物与手的位置、船沿和座板接触。窄屏操作使用鼠标模拟，未连接真实手机。
- 猫的最新页面检查没有浏览器错误或警告。另复查了修正后的羽毛棒显示。
- 13 个顶层 HTML 的本地文件引用和重复 ID 检查无问题；外部 JavaScript、11 段内联 JavaScript、2 个 JSON 场景清单语法检查通过。角色生成脚本重复运行结果不变。
- 本轮对修改后的 JavaScript/CSS 运行 Impeccable 静态扫描，未报告问题。该扫描不代替视觉检查。
- 部署 ZIP 完整性通过，共 24 个运行时文件，约 2.29 MiB。写实手已归档，不再加载或打包。

## 范围

这是二维绘本动画，角色轮廓通过连续形变保持连接，并使用固定前景遮罩和道具锚点约束位置；并非三维碰撞模拟。本轮验证覆盖上述采样动作与实际拖动，不能据此宣称任意操作组合下绝无遮挡问题。WebGL 不可用时保留完整静态角色及表情反馈，避免显示断开的部件。声音引擎本轮未更改；之前的音频输出验证不是本轮视觉验证的依据。
